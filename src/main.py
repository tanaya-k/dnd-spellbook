import json

with open('spell-list.json') as spell_list:
    data = json.load(spell_list)

# Input format, classType is the varibale that can be used with print now
# classType = input("Please select your class:")


# Generate jsons for the different classes
